import React from 'react';
import SearchBar from '../../UI/search-bar';


const Search = () => {

    return <div>
        <SearchBar placeholder={ '搜索' } type={ 'search' } />
    </div>
}

export default Search;